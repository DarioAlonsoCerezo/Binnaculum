# Copilot SQL Skill Mapping

Canonical source:

- `.ai/registry/skills/sql-change.yaml`

Copilot execution notes:

- Apply repository SQL module patterns (`createTable`, `insert`, `update`, `delete`, `get*`).
- Use semantic-first discovery when available, then exact search fallback.
- Follow `.ai/core/sql-guidelines.md` for constraints and validation.

Canonical agent mapping:

- `.ai/registry/agents/sql-impact-analyzer.yaml`
